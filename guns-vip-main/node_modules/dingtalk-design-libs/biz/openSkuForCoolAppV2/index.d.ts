interface IOpenSkuForCoolAppOpt {
    corpId: string;
    coolAppCode: string;
    getCoolAppStatusMiddleware: (callBack: (data: any) => void) => void;
    openConversationId?: string;
    source?: string;
}
/**
 * @name openSkuForCoolAppV2
 * @category biz Helpers
 * @summary 在应用内调起酷应用快捷授权
 * @description
 *
 * @param {Object} opt - 打开参数配置
 * @param {String} [opt.corpId] - 组织ID
 * @param {String} [opt.coolAppCode] - 酷应用code
 * @param {String} [opt.openConversationId] - 群会话的openConversationId
 * @returns {Promise}
 */
export declare function openSkuForCoolAppV2(opt: IOpenSkuForCoolAppOpt): Promise<unknown>;
export {};
