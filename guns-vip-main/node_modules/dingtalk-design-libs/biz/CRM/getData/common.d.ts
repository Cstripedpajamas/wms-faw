/**
 * 参数
 */
export interface ICRMUrlParams {
    corpId: string;
    upgradeType: string;
    suiteKey: string;
    instanceId: string;
    isPre?: boolean;
}
/**
 * 获取下客户管理授权页的url
 * @param isPre
 */
export declare function getCRMAuthUrl(params: ICRMUrlParams): string;
/**
 * 检测某条数据有没有授权好
 * @param corpId 企业id
 * @param suiteKey isv的suiteKey
 * @param instanceId 实例id
 * @returns
 */
export declare const checkHasAuth: (corpId: string, suiteKey: string, instanceId: string) => Promise<unknown>;
