export interface IGetCRMDataProps {
    corpId: string;
    instanceId: string;
    upgradeType: string;
    suiteKey: string;
}
/**
 * 客户管理授权客户敏感数据前端api，调用后会在钉钉端内打开
 * @category biz/CRM
 * @module biz/CRM
 * @param {Object} params 参数
 * @param {string} params.corpId 企业id
 * @param {string} params.instanceId 数据id
 * @param {string} params.upgradeType 开放平台upgradeType
 * @param {string} params.suiteKey 开放平台ISV所使用的suiteKey，以‘suite’开头
 * @support pc web
 * @returns {Promise} 成功、失败
 * @example
 * getDataFromCRM({
 *     corpId: 'dingxxxxxx',
 *     instanceId: 'exxx-axxxx-xxxxxx-xxxxx',
 *     upgradeType: 'xxxxx',
 *     suiteKey: 'suitexxxx',
 * }).then(() => {
 *     toast('授权成功', 'success');
 * }).catch(() => {
 *     // 用户已经取消
 * });
 */
export declare function getDataFromCRM(params: IGetCRMDataProps): Promise<unknown>;
